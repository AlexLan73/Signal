# SignalAnalyzer - Desktop Signal Analysis Tool

## 🎯 Описание

SignalAnalyzer - это настольное приложение для анализа сигналов с системой стробов и лучей. Приложение позволяет генерировать, анализировать и визуализировать математические сигналы в реальном времени.

## ✨ Возможности

### 🎛️ Система стробов и лучей
- **Генерация стробов** с любым количеством лучей
- **8 типов сигналов**: sine, cosine, square, sawtooth, triangle, quadrature, modulated, pulse
- **Модуляция**: AM, FM, PM с настраиваемыми параметрами
- **Гармоники**: множественные синусоиды с разными амплитудами и фазами
- **Шум**: 5 типов с настраиваемым уровнем (белый гауссов, цветной, равномерный, розовый, коричневый)
- **Визуализация**: временная область, FFT, спектрограмма, фаза
- **Сохранение/загрузка** стробов в JSON/NPY формате

### 🧮 Математический движок
- **Генерация сигналов** с валидацией параметров
- **FFT анализ** и спектральная плотность
- **Статистика**: RMS, пик-пик, коэффициент амплитуды, эксцесс, асимметрия
- **Поиск пиков** в сигналах
- **Фильтрация** и обработка сигналов

### 🎨 GUI интерфейс
- **PyQt6** с русской локализацией
- **Модульная архитектура** с разделением на слои
- **Вкладки**: Генератор сигналов, 2D график, 3D график, Спектр, Осциллограф, Стробы
- **Интерактивная визуализация** с matplotlib
- **Система событий** для связи компонентов

## 🚀 Быстрый запуск

### Установка зависимостей
```bash
pip install PyQt6 matplotlib numpy scipy sympy loguru
```

### Запуск приложения
```bash
# Основное GUI приложение
python run_main_gui.py

# Тест системы стробов
python run_strobe_test.py

# Тест математического движка
python run_math_test.py

# Полный интеграционный тест
python run_full_test.py
```

## 🎛️ Использование системы стробов

### 1. Конфигурация строба
- **Общая длина**: L точек (например, 3072)
- **Количество лучей**: N (например, 3)
- **Точек на луч**: 2^n (например, 1024)
- **Частота дискретизации**: например, 100 кГц

### 2. Конфигурация лучей
- **Тип сигнала**: sine, cosine, square, sawtooth, triangle, quadrature, modulated, pulse
- **Частота**: в Гц
- **Амплитуда**: в Вольтах
- **Фаза**: в градусах
- **DC смещение**: в Вольтах
- **Гармоники**: дополнительные синусоиды
- **Шум**: тип и уровень

### 3. Тестовый строб (3 луча)
- **Луч 0**: синусы с периодом 300 и 200 точек, фазы 0° и +30°
- **Луч 1**: синусы с периодом 250 и 150 точек, фазы 0° и +40°
- **Луч 2**: синусы с периодом 200 и 100 точек, фазы 0° и +50°
- **Шум**: гауссов 0.2 для всех лучей

## 📁 Структура проекта

```
Signal/
├── src/                          # Исходный код
│   ├── gui/                     # GUI компоненты
│   │   ├── main_window.py       # Главное окно
│   │   ├── strobe_widget.py     # Виджет стробов
│   │   └── localization.py      # Русская локализация
│   ├── math/                    # Математический движок
│   │   ├── signal_generator.py  # Генератор сигналов
│   │   └── strobe_system.py     # Система стробов
│   ├── visualization/           # Визуализация
│   │   ├── matplotlib_2d.py     # 2D графики
│   │   └── strobe_plot.py       # Визуализация стробов
│   └── utils/                   # Утилиты
│       └── logger.py            # Логирование
├── tests/                       # Тесты
├── data/                        # Данные
├── logs/                        # Логи
├── specs/                       # Спецификации
├── run_*.py                     # Файлы запуска
└── pyproject.toml               # Конфигурация проекта
```

## 🔧 Настройка PyCharm

### 1. Открытие проекта
1. Откройте PyCharm Community Edition
2. File → Open → выберите папку проекта
3. Дождитесь индексации

### 2. Настройка Run Configurations
В папке `.idea/runConfigurations/` уже настроены конфигурации:
- `SignalAnalyzer_GUI.xml` - основное приложение
- `Strobe_System_Test.xml` - тест системы стробов
- `Math_Engine_Test.xml` - тест математического движка
- `Full_Integration_Test.xml` - полный интеграционный тест

### 3. Запуск в PyCharm
1. Выберите конфигурацию в dropdown меню
2. Нажмите Run (Ctrl+Shift+F10) или Debug (Shift+F9)
3. Для отладки установите breakpoints

## 🧪 Тестирование

### Запуск тестов
```bash
# Все тесты
pytest

# Только unit тесты
pytest tests/unit/

# Только GUI тесты
pytest tests/gui/

# С покрытием кода
pytest --cov=src --cov-report=html
```

### Типы тестов
- **Unit тесты**: тестирование отдельных функций и классов
- **Integration тесты**: тестирование взаимодействия компонентов
- **GUI тесты**: тестирование интерфейса с pytest-qt
- **Performance тесты**: тестирование производительности

## 📊 Визуализация

### Основной график
- Отображение всех лучей в одном графике
- Разделители между лучами
- Подписи для каждого луча

### FFT анализ
- Частотная область
- Спектральная плотность
- Гармонические компоненты

### Выбранный луч
- Детальный просмотр одного луча
- Фазовые характеристики
- Временная область

### Спектрограмма
- Время-частотное представление
- Цветовая карта интенсивности
- Динамические характеристики

## 🎨 Особенности GUI

### Русская локализация
- Все надписи переведены на русский
- Автоматический перевод виджетов
- Функция `tr()` для перевода текста

### Модульная архитектура
- Разделение на слои: GUI, Visualization, Math, Data
- Observer pattern для связи компонентов
- Loose coupling между модулями

### Система событий
- PyQt6 сигналы для связи компонентов
- Обновление в реальном времени
- Асинхронная обработка

## 🔧 Разработка

### Установка для разработки
```bash
git clone <repository>
cd Signal
pip install -e ".[dev]"
```

### Форматирование кода
```bash
black src/ tests/
isort src/ tests/
flake8 src/ tests/
mypy src/
```

### Структура кода
- **TDD подход**: тесты пишутся перед кодом
- **Type hints**: аннотации типов для всех функций
- **Docstrings**: документация для всех классов и методов
- **Логирование**: структурированное логирование с loguru

## 📈 Производительность

### Оптимизации
- **NumPy**: векторизованные операции
- **Matplotlib**: эффективная отрисовка
- **PyQt6**: нативный GUI
- **Кэширование**: повторное использование вычислений

### GPU ускорение (опционально)
```bash
pip install cupy-cuda12x  # Для NVIDIA GPU
```

## 🐛 Устранение неполадок

### Ошибки импорта
```bash
# PyQt6
pip install PyQt6

# matplotlib
pip install matplotlib

# numpy
pip install numpy
```

### Проблемы с путями
- Убедитесь, что рабочая директория: корень проекта
- Проверьте, что все файлы присутствуют
- Проверьте PYTHONPATH

### Проблемы с отображением
- Проверьте, что установлен правильный Python
- Убедитесь, что все зависимости установлены
- Проверьте логи в папке `logs/`

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch
3. Добавьте тесты для новой функциональности
4. Убедитесь, что все тесты проходят
5. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки PyCharm
4. Запустите тесты для диагностики

## 🎉 Готово!

Теперь вы можете:
- Запускать SignalAnalyzer в PyCharm
- Отлаживать код с помощью breakpoints
- Тестировать различные компоненты
- Разрабатывать новые функции
- Работать с системой стробов

Удачи в разработке! 🚀
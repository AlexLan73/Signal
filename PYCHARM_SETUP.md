# Настройка SignalAnalyzer в PyCharm

## 🚀 Быстрый запуск

### 1. Основное GUI приложение
```bash
# Запуск основного приложения
python run_main_gui.py
```

### 2. Тест системы стробов
```bash
# Запуск теста системы стробов
python run_strobe_test.py
```

### 3. Тест математического движка
```bash
# Запуск теста математического движка
python run_math_test.py
```

### 4. Полный интеграционный тест
```bash
# Запуск полного интеграционного теста
python run_full_test.py
```

## ⚙️ Настройка PyCharm

### 1. Открытие проекта
1. Откройте PyCharm Community Edition
2. File → Open → выберите папку `E:\Signal`
3. Дождитесь индексации проекта

### 2. Настройка Python интерпретатора
1. File → Settings → Project → Python Interpreter
2. Убедитесь, что выбран правильный Python (Python 3.12+)
3. Если нужно, добавьте интерпретатор: ⚙️ → Add → Existing Environment

### 3. Установка зависимостей
1. Откройте Terminal в PyCharm (View → Tool Windows → Terminal)
2. Установите зависимости:
```bash
pip install PyQt6 matplotlib numpy scipy sympy
```

### 4. Настройка Run Configurations

#### Основное GUI приложение
1. Run → Edit Configurations
2. Нажмите "+" → Python
3. Настройки:
   - **Name**: `SignalAnalyzer GUI`
   - **Script path**: `E:\Signal\run_main_gui.py`
   - **Working directory**: `E:\Signal`
   - **Python interpreter**: ваш Python 3.12+

#### Тест системы стробов
1. Run → Edit Configurations
2. Нажмите "+" → Python
3. Настройки:
   - **Name**: `Strobe System Test`
   - **Script path**: `E:\Signal\run_strobe_test.py`
   - **Working directory**: `E:\Signal`

#### Тест математического движка
1. Run → Edit Configurations
2. Нажмите "+" → Python
3. Настройки:
   - **Name**: `Math Engine Test`
   - **Script path**: `E:\Signal\run_math_test.py`
   - **Working directory**: `E:\Signal`

#### Полный интеграционный тест
1. Run → Edit Configurations
2. Нажмите "+" → Python
3. Настройки:
   - **Name**: `Full Integration Test`
   - **Script path**: `E:\Signal\run_full_test.py`
   - **Working directory**: `E:\Signal`

## 🎯 Возможности приложения

### Основное GUI приложение
- **Главное окно** с меню и панелью инструментов
- **Вкладки**: Генератор сигналов, 2D график, 3D график, Спектр, Осциллограф, **Стробы**
- **Русская локализация** интерфейса
- **Система стробов** с конфигурацией лучей

### Система стробов
- **Генерация стробов** с любым количеством лучей
- **8 типов сигналов**: sine, cosine, square, sawtooth, triangle, quadrature, modulated, pulse
- **Модуляция**: AM, FM, PM
- **Гармоники**: множественные синусоиды
- **Шум**: 5 типов с настраиваемым уровнем
- **Визуализация**: временная область, FFT, спектрограмма, фаза
- **Сохранение/загрузка** стробов

### Математический движок
- **Генерация сигналов** с валидацией параметров
- **FFT анализ** и спектральная плотность
- **Статистика**: RMS, пик-пик, коэффициент амплитуды
- **Поиск пиков** в сигналах
- **Фильтрация** и обработка сигналов

## 🐛 Отладка в PyCharm

### 1. Установка точек останова
1. Кликните слева от номера строки для установки breakpoint
2. Запустите в режиме отладки (Shift+F9)
3. Используйте F8 для пошагового выполнения

### 2. Просмотр переменных
1. В окне отладки (Debug) просматривайте переменные
2. Используйте Evaluate Expression (Alt+F8) для вычисления выражений
3. Просматривайте call stack для понимания потока выполнения

### 3. Логирование
- Логи сохраняются в папке `logs/`
- Используйте logger для отладки:
```python
from src.utils.logger import setup_logger
logger = setup_logger(__name__)
logger.info("Отладочная информация")
```

## 📁 Структура проекта

```
E:\Signal\
├── src/                          # Исходный код
│   ├── gui/                     # GUI компоненты
│   │   ├── main_window.py       # Главное окно
│   │   ├── strobe_widget.py     # Виджет стробов
│   │   └── localization.py      # Русская локализация
│   ├── math/                    # Математический движок
│   │   ├── signal_generator.py  # Генератор сигналов
│   │   └── strobe_system.py     # Система стробов
│   ├── visualization/           # Визуализация
│   │   ├── matplotlib_2d.py     # 2D графики
│   │   └── strobe_plot.py       # Визуализация стробов
│   └── utils/                   # Утилиты
│       └── logger.py            # Логирование
├── tests/                       # Тесты
├── data/                        # Данные
├── logs/                        # Логи
├── specs/                       # Спецификации
└── run_*.py                     # Файлы запуска
```

## 🎨 Особенности GUI

### Русская локализация
- Все надписи переведены на русский
- Автоматический перевод виджетов
- Функция `tr()` для перевода текста

### Система стробов
- **Левая панель**: конфигурация стробов и лучей
- **Правая панель**: визуализация в реальном времени
- **Вкладки лучей**: настройка параметров каждого луча
- **Тестовые шаблоны**: готовые конфигурации

### Визуализация
- **Основной график**: все лучи с разделителями
- **FFT**: частотная область
- **Выбранный луч**: детальный просмотр
- **Фаза**: фазовые характеристики
- **Спектрограмма**: время-частота

## 🔧 Устранение неполадок

### Ошибка импорта PyQt6
```bash
pip install PyQt6
```

### Ошибка импорта matplotlib
```bash
pip install matplotlib
```

### Ошибка импорта numpy
```bash
pip install numpy
```

### Проблемы с путями
- Убедитесь, что рабочая директория: `E:\Signal`
- Проверьте, что все файлы присутствуют

### Проблемы с отображением
- Проверьте, что установлен правильный Python интерпретатор
- Убедитесь, что все зависимости установлены
- Проверьте логи в папке `logs/`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки PyCharm
4. Запустите тесты для диагностики

## 🎉 Готово!

Теперь вы можете:
- Запускать SignalAnalyzer в PyCharm
- Отлаживать код с помощью breakpoints
- Тестировать различные компоненты
- Разрабатывать новые функции
- Работать с системой стробов

Удачи в разработке! 🚀